# =============================================================================
# CADDY PROJECT CONFIGURATION
# =============================================================================
# Copy this file to caddy.env
#
# Format: domain:network[:caddyfile_path]
#   - domain: Project domain (required) - e.g., lichcupdienvn.com
#   - network: Docker network name (required) - e.g., lcdvn-network
#   - caddyfile_path: Path to project's Caddyfile (optional)
#
# Examples:
#   PROJECTS="lichcupdienvn.com:lcdvn-network:/root/lcdvn/Caddyfile.static"
#   PROJECTS="projecta.com:projecta-network projectb.com:projectb-network:/root/projectb/Caddyfile"
#
# For each project, bootstrap.sh will:
#   1. Connect Caddy to the project's network
#   2. Copy Caddyfile to caddy-projects/{domain}.caddy (or create empty)
#   3. Append syslog.{domain} config for logging access

PROJECTS=""

# =============================================================================
# ADDING A NEW PROJECT
# =============================================================================
# 1. Ensure the project's docker-compose.yml defines network with explicit name:
#
#    networks:
#      myproject-network:
#        name: myproject-network    # <-- important!
#        driver: bridge
#
# 2. Add the project to PROJECTS above
# 3. Run: ./bootstrap.sh
# 4. Caddy will be reloaded automatically
